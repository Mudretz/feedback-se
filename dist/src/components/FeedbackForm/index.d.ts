import { FC } from 'react';
import { FeedBackProps, Stages } from '../../shared/types';

interface Props extends FeedBackProps {
    item: Stages;
}
export declare const FeedbackForm: FC<Props>;
export {};
